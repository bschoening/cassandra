The latest major version is 5.1 version and the corresponding distribution name is `51x` (with an "x" as the suffix).
For older releases use:

* `51x` for C* 5.1 version series
* `50x` for C* 5.0 version series
* `41x` for C* 4.1 version series

For example, to add the repository for version 5.1 version (`51x`):
